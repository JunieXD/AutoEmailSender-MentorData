# 贡献指南

普通贡献者应使用仓库的 GitHub Issue Forms：

- 上传导师表格（推荐）
- 贡献一位导师（手动填写）
- 反馈导师信息问题

请提供能够支持相关字段的高校官方页面。搜索引擎摘要、聚合站、社交媒体、模型生成内容和未经学校确认的私人主页不能作为最终证据。

对于你有权许可的投稿内容，你同意按照 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) 提供，并允许项目公开保存 GitHub 数字用户 ID、提交时用户名和 Issue URL 作为归因与审计记录。高校官网等第三方来源仍保留其原有权利。

批量投稿请在 Auto Email Sender 中点击“贡献到社区”并保存导出的文件，再把文件拖入“上传导师表格”。等待 GitHub 显示附件链接后即可提交。建议每次提交同一所学校、同一个学院的数据，审核会更快；需要时，一个文件也可以包含多个学校和学院（仍受 5,000 行总上限约束）。机构名称请尽量使用官网全称，简称也可以提交并由维护者统一。原始附件只用于受限解析，不会写入 Git 仓库；系统会按学校、学院和系所写法聚合审核，同时保留每一行的独立来源。

普通数据错误（例如个别行缺少姓名、邮箱或来源）只会使该行退出提案并出现在审核摘要中，不会拖累同一文件的其他有效行。公式、危险 XLSX 结构、错误表头、超限文件等安全问题仍会拒绝整个文件。

直接修改数据文件的 Pull Request 只面向维护者和已经明确授权的可信贡献者。普通投稿者无需接触 JSON、Git 或 Actions。

提交前请确认：

- 邮箱是公开职业邮箱而非私人邮箱；
- 一行只描述一位导师；
- 学校、学院和系所尽量使用官网正式名称；简称也可以提交，但会由维护者统一映射；
- 不确定的信息保持为空，不要猜测；
- `source_url` 指向支持该信息的具体官方页面；
- 没有包含个人备注、任务、发送记录或匹配数据。
