# 社区导师数据政策（发布前草案）

## 数据范围

本仓库只收录高校、学院、系所、研究院和实验室官网已经公开的职业信息，包括姓名、公开工作邮箱、职称、任职机构、研究方向、代表论文标题、个人主页和证据来源页。

不得提交私人邮箱、电话号码、家庭住址、身份证明、学生信息、私人备注、发送记录、任务信息、匹配结果或非公开材料。不得复制完整简历、人物简介、论文正文或大段网页内容。

## 投稿授权与归因

投稿者确认其提交的是公开职业信息，并同意本项目保存其公开 GitHub 数字用户 ID、提交时用户名、Issue URL 和提交时间，用于归因、审计、纠错与滥用处置。

每条被接受的贡献都保留原始提交值和最终采用值。自动归一化或维护者修改不会覆盖原始 Issue。公开数据包可以展示贡献者用户名和 Issue 链接。

## 更正、退出和撤销

- 任何人都可以通过错误反馈 Issue 提供新的官方证据。
- 反馈本身不会直接修改或下架数据。
- 确认过时的信息标记为 `superseded`，不视为原投稿者恶意。
- 确认伪造的贡献可以撤销，并可封禁贡献者数字 ID。
- 导师本人或机构提出合理删除请求时，维护者可以将记录标记为 `removed` 并从当前发布数据撤下。
- Git 历史可能保留旧值；项目不承诺重写既有 Git 历史。

## 数据状态

发布数据可能具有以下状态：

- `active`：当前可用于社区导入。
- `retired`：已退休，默认不出现在可导入列表中，但保留历史记录。
- `departed`：已离开当前机构，默认不出现在该机构的可导入列表中。
- `deceased`：已确认去世，不可导入。
- `stale`：长期未核验或来源失效，只能在用户明确选择时查看。
- `disputed`：存在尚未解决的关键冲突，不可自动导入。
- `removed`：因删除请求、伪造或政策原因从当前数据集撤下。

软件不得仅凭远端状态静默删除用户本地数据。它只能提示更新、撤销或归档建议。

## 许可证

程序代码使用 MIT License。社区数据的最终开放数据许可证在首次公开发布前确定；候选方案为 CC BY 4.0 或 ODC-By 1.0。正式许可证确定前，本仓库不得宣称第三方可以无条件再发布全部数据。

